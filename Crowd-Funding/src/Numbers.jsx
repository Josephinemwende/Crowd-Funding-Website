import { useState } from 'react';
function trackNumbers(){
    const[ trackedProjects, setTrackedProjects ] = useState(0);
    const[ totalBackers, setTotalBackers ] = useState(0);
    const[ remainingDates, setRemainingDates ] = useState(0);
} 
function Numbers(){
    return(
        <>
            <div></div>
        </>
    );
}
export default Numbers;